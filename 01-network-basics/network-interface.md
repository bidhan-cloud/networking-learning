# NETWORK INTERFACE

## What is it?

A network interface is the connection point between a device and a network.

## Real examples

eth0 → Ethernet connection  
wlan0 → Wi-Fi connection

## Why do we need it?

Without a network interface, a device cannot send or receive data.

## What I learned

Network interface connects my device to a network.
