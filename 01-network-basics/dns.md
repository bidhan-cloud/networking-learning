# DNS

## What is it?

DNS translates domain names into IP addresses.

Example:

google.com → 142.x.x.x

## Why do we need it?

Humans remember names easier than numbers.

## What I learned

DNS helps me access websites using names instead of IP addresses.
